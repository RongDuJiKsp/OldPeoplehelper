#include "OldPeopleHelper.h"
int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    Ui_MainWindow mainwindow;
    mainwindow.setWindowTitle("A way to Use PC");
    mainwindow.show();
    return a.exec();
}
